[components]

target_pos = { type = "Vec2", name = "target_pos", description = "target_pos", attributes = [
    "Debuggable",
    "Networked",
] }

spawn_timer = { type = "F32", name = "spawn_timer", description = "How many seconds are left before creeps are spawned", attributes = [
    "Debuggable",
    "Resource",
]}

is_creep = { type = "EntityId", name = "is_creep", description = "a creep, a NPC that is on the side of one of the teams", attributes = [
    "Debuggable",
] }

team = { type = "U32", name = "Team", desription = "0=>Mars, 1=>Jupiter, 2=>... As role seems to be made for heroes, team is a more generic component for creeps.", attributes = [
    "Debuggable",
    "Networked",
]}